Application allows the user to build their own custom PC, choosing the various PC parts/
componenets they desire.

Local Storage is used to keep track of user progress. This is being done by user clicking a button which saves their session
which stores the user details, pc components and totals so that incase of an accidental exit of the page, their session will be restored.

